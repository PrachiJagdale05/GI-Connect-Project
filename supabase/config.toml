
project_id = "jumcsxhftlhxzmeqpuvb"

[api]
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
port = 54323

[storage]
file_size_limit = "50MiB"

[auth]
site_url = "http://localhost:3000"
additional_redirect_urls = ["https://localhost:3000"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
otp_expiry = 120 # Reduced OTP expiry time to 120 seconds (2 minutes)
enable_leaked_password_protection = true # Enable protection against leaked passwords

[inbucket]
pop3_port = 54324
smtp_port = 54325
web_port = 54326

[vector]
port = 54327

[storage.images]
allowed_mime_types = ["image/png", "image/jpeg", "image/gif", "image/webp"]
file_size_limit = "10MiB"

[storage.product-videos]
allowed_mime_types = ["video/mp4", "video/webm", "video/quicktime"]
file_size_limit = "50MiB"

[functions.trigger-ai]
verify_jwt = true

[functions.get_notifications]
verify_jwt = true

[functions.update_notification_read_status]
verify_jwt = true

[functions.chatbot-proxy-v2]
verify_jwt = false
